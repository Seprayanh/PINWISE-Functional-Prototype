PINWISE HiFi Interaction Upgrade

Added:
1. DecisionBar
   Unified Keep / Review / Remove interaction.

2. ConfidenceMeter
   Visual AI confidence indicator.

3. InformationDiff
   Saved vs Recent comparison component.

Integration:
Replace duplicated decision controls and freshness comparison UI.

After testing:
git add .
git commit -m "Improve decision consistency and AI transparency UI"
git push origin main
