FINAL INTEGRATION NOTE

Modify the existing src/types.ts (do not replace blindly).

Inside Place type add:

image?: string
thumbnail?: string
evidenceImage?: string

Keep all existing:
- PlaceDecision
- Evidence types
- StudyCondition
- Trip data structures
